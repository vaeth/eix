# @SYSCONFDIR@/eix-sync.conf
#
# In this file and in the environment variable EIX_SYNC_OPTS you can store
# default options for eix-sync. Both are evaluated, so you can use
# shell functions to calculate arguments. Be sure to quote correctly.
# Moreover, you can also use hooks to call layman or update some caches.
# See "man eix" for more details.
# Just one example: On a machine which gets its PORTDIR from another machine
# you might want to put here the line
# -uT
